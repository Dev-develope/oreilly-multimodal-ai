livekit-agents>=0.12.1
livekit-plugins-silero>=0.7.4
python-dotenv~=1.0
aiohttp>=3.9
# The 60dB provider (./sixtydb) is a local plugin and needs no extra package.
# livekit-plugins-openai / -deepgram are no longer required, but keep them
# installed if you want to fall back to those providers.
